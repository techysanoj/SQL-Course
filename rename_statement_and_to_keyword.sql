use temp_db;

-- to show all the tables
show tables;

-- to rename table name from a to b
rename table employee to emp;

rename table emp to abc; -- if the table with the name (after to) already exist then it will give error
